
using System.Collections.Generic;
using EPiServer.Core;
using EPiServer.Shell.ObjectEditing;
namespace Freshfields.Web.Models.Properties;

public class LocationSelectionFactory //: ISelectionFactory
{
    /*public IEnumerable<ISelectItem> GetSelections(ExtendedMetadata metaData)
    {
        var values = SelectItemHelper.GetLocationList;
        values.Insert(0, new SelectItem { Text = string.Empty, Value = ContentReference.EmptyReference });
        return values;
    }*/
}
