import { ContextMode } from '@episerver/content-delivery';

import BackButtonProps from './BackButton.props';

const mockdata: BackButtonProps = {
    previousUrl: '',
    mode: ContextMode.Default,
};

export default mockdata;
