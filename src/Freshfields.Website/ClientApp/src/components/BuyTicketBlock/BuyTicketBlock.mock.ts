import BuyTicketBlockProps from './BuyTicketBlock.props';

const mockdata: BuyTicketBlockProps = {
    heading: 'Heading',
    message: 'This is the message',
    pagePropertyName: 'buy-ticket-block',
};

export default mockdata;
